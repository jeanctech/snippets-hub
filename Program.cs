using System;
using System.ComponentModel.DataAnnotations;

class Program
{
    static void Main()
    {
        int age_int = 20;
        Console.WriteLine(age_int);

        double age_float = 20.3;
        Console.WriteLine(age_float);

        bool age_bool = false;
        Console.WriteLine(age_bool);

        bool age_boolean = false;
        Console.WriteLine(age_boolean);

        string age_name = "user";
        Console.WriteLine(age_name);
    }
}